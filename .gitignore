# compiled output
/dist
/node_modules

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Environment — .env с реальными секретами не коммитим
.env
.env.local
.env.*.local

# Локальная SQLite БД (если использовалась в dev)
*.db
*.db-journal
dev.db
dev.db-journal

# Загрузки пользователей — только сохраняем структуру папок
uploads/*
!uploads/.gitkeep
!uploads/*.jpg
!uploads/*.png
!uploads/*.gif
!uploads/avatars/
uploads/avatars/*
!uploads/avatars/.gitkeep
!uploads/avatars/*.jpg
!uploads/avatars/*.png
!uploads/avatars/*.gif

# Backups
backups/
*.dump
*.backup
*.tar.gz
*.zip
